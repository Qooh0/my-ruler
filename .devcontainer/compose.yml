# Generated by qadiff-dct
# 共通設定（チーム共有可能）
# ユーザー固有のマウント設定は compose.override.yml で定義
name: my-ruler
services:
  dev:


    build:
      context: .
      dockerfile: Dockerfile
      args:
        TZ: ${TZ:-Asia/Tokyo}
        CLAUDE_CODE_VERSION: latest
        BASE_IMAGE: mcr.microsoft.com/devcontainers/base:ubuntu

    volumes:
      # ワークスペースマウントは compose.override.yml で設定（ユーザー固有のパス）
      - commandhistory:/commandhistory
    command: sleep infinity
    cap_add:
      - NET_ADMIN
      - NET_RAW




volumes:
  commandhistory: {}

