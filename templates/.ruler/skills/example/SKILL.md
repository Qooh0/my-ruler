---
name: example
description: Example skill distributed by my-ruler. Replace this with your own skill.
---

# Example Skill

This is an example skill distributed via `my-ruler sync`.

## Usage

Invoke with `/example` or let the AI agent use it automatically based on the description above.

## Customization

Edit this file in `templates/.ruler/skills/example/SKILL.md` of your my-ruler package,
then run `my-ruler sync <target>` to distribute it to your projects.
